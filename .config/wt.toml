# Project config — committed; per-user overrides in ~/.config/worktrunk/config.toml.
# `.worktreeinclude` (repo root) controls which gitignored paths get seeded.

[post-start]
copy-caches = "wt step copy-ignored"
tools       = "make tools"

[pre-merge]
verify = "make verify"

[pre-remove]
status = "git status --short"
