version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosec
    - gocritic
    - revive
    - ineffassign
    - misspell

formatters:
  enable:
    - gofumpt
    - goimports

run:
  timeout: 5m
