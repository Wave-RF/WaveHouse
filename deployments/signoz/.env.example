# SigNoz internal ClickHouse credentials.
# Copy this file to `.env` (gitignored) and override for non-dev use.
# Defaults below match the compose file's fallbacks, so unset works for local dev.
SIGNOZ_CH_USER=default
SIGNOZ_CH_PASSWORD=password
